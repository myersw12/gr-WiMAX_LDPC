# auto-generated by grc.converter

id: wimaxldpc_ldpc_decoder
label: ldpc_decoder
category: '[WiMAX_LDPC]'

parameters:
-   id: rate
    label: Code Rate
    dtype: int
    options: ['0', '1', '2', '3', '4', '5']
    option_labels: [Half Rate, Two Thirds A, Two Thirds B, Three Quarters A, Three
            Quarters B, Five Sixths]
-   id: z
    label: Z Factor
    dtype: int
    default: '96'
-   id: max_iterations
    label: Max Iterations
    dtype: int
    default: '10'
-   id: soft
    label: Soft Input
    dtype: bool
    options: ['True', 'False']
    option_labels: ['Yes', 'No']
-   id: num_threads
    label: Number of Threads
    dtype: int
    options: ['1', '2', '4', '6', '8', '16']

inputs:
-   domain: message
    id: in
    optional: true

outputs:
-   domain: message
    id: out
    optional: true

templates:
    imports: import WiMAX_LDPC
    make: WiMAX_LDPC.ldpc_decoder(${rate}, ${z}, ${max_iterations}, ${soft}, ${num_threads})

file_format: 1
